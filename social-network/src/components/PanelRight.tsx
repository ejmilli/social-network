import React from "react";

const PanelRight: React.FC = () => (
  <aside style={{ width: 220, padding: 16, borderLeft: "1px solid #eee" }}>
    {/* Future: UserList, chat users, etc. */}
    <div style={{ color: "#aaa", fontSize: 14 }}>
      (User list and chat coming soon)
    </div>
  </aside>
);

export default PanelRight;
